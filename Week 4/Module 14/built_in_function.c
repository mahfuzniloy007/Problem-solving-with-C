#include <stdio.h>
#include <math.h>
#include <stdlib.h>
int main()
{
    // double x;
    // scanf("%lf", &x);
    // int ans = ceil(x);[x er uporer value nibe]
    // int ans = floor(x);[x er nicher value nibe]
    // int ans = round(x);[x er value < 3.5 hole nicher ta otherwise uporer ta]


    // int x;
    // scanf("%d", &x);
    // double ans = sqrt(x);[for root]
    // printf("%0.2lf", ans);

    // int x, y;
    // scanf("%d%d", &a, &b);
    // int ans = pow(a, b);[for power]
    // printf("%d", ans);

    // [for absoulate value]
    int x;
    scanf("%d", &x);
    int ans = abs(x);
    printf("%d", ans);
    return 0;
}